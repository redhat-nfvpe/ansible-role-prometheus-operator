apiVersion: v1
kind: Service
metadata:
  labels:
    prometheus: {{ promops_deploy.name }}
  name: prometheus-{{ promops_deploy.name }}
spec:
  type: NodePort
  ports:
  - name: web
    nodePort: 30900
    port: 9090
    protocol: TCP
    targetPort: web
  selector:
    prometheus: {{ promops_deploy.name }}
