apiVersion: v1
kind: Service
metadata:
  name: prometheus-operator
  labels:
    {{ promops_deploy.name }}-app: prometheus-operator
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    {{ promops_deploy.name }}-app: prometheus-operator
